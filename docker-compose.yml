services:
  wuhuan-training-agent:
    build: .
    ports:
      - "3017:3000"
    environment:
      NODE_ENV: production
      WUHUA_DATA_DIR: /data
    volumes:
      - wuhuan_data:/data
    restart: unless-stopped

volumes:
  wuhuan_data:
